# Безопасные маршруты - Telegram Mini App

Telegram Mini App для построения безопасных пешеходных маршрутов с возможностью исключения запретных зон.

## Технологии

- **React + TypeScript** - UI фреймворк
- **Vite** - сборщик
- **@tma.js/sdk-react** - интеграция с Telegram Mini Apps
- **MapLibre GL** - карты
- **OpenStreetMap** - тайлы карт
- **OpenRouteService** - маршрутизация с поддержкой avoid_polygons

## Установка

```bash
npm install
```

## Настройка

1. Создайте файл `.env` в корне проекта (можно скопировать из `.env.example`)
2. Получите бесплатный API ключ на [OpenRouteService](https://openrouteservice.org/dev/#/signup)
3. Настройте переменные окружения в `.env`:

```env
# OpenRouteService API ключ для построения маршрутов
VITE_ORS_API_KEY=your_api_key_here

# Базовый URL API бэкенда
VITE_API_BASE_URL=https://your-backend.com/api

# Секретный ключ для авторизации (используется в заголовке Authorization Bearer при запросе авторизации)
VITE_AUTH_SECRET_KEY=your_secret_key_here
```

**Важно:** 
- Без API ключа OpenRouteService построение маршрутов не будет работать
- Без настроенных `VITE_API_BASE_URL` и `VITE_AUTH_SECRET_KEY` авторизация не будет работать (в режиме отладки авторизация пропускается)
- В режиме разработки используется прокси Vite для обхода CORS

## Запуск

### Локальная разработка

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Предпросмотр production билда

```bash
npm run build
npm run preview
```

## Функционал

- ✅ Построение маршрута от точки A до точки B
- ✅ Выбор точек на карте кликом
- ✅ Использование текущей геолокации для точек A и B (кнопки 📍)
- ✅ Добавление запретных зон (круг заданного радиуса)
- ✅ Исключение запретных зон из маршрута
- ✅ Отображение маршрута и зон на карте
- ✅ Информация о расстоянии и времени маршрута
- ✅ Редактирование радиуса запретных зон

## Использование

1. Выберите точку A на карте (кликом) или используйте кнопку 📍 для текущей геолокации
2. Выберите точку B на карте (кликом) или используйте кнопку 📍 для текущей геолокации
3. При необходимости добавьте запретные зоны:
   - Нажмите "Добавить зону"
   - Кликните на карте в центре зоны
   - Отредактируйте радиус зоны при необходимости
4. Нажмите "Построить маршрут"
5. Маршрут будет построен с учетом всех запретных зон

## Развертывание

### GitHub Pages (автоматический деплой)

1. Перейдите в **Settings → Secrets and variables → Actions** вашего репозитория
2. Добавьте секрет `VITE_ORS_API_KEY` с вашим API ключом OpenRouteService
3. Перейдите в **Settings → Pages**
4. В разделе **Source** выберите **GitHub Actions**
5. При каждом push в ветку `main` приложение автоматически соберется и задеплоится

**Важно:** Убедитесь, что в настройках Pages включен **Enforce HTTPS**.

### Ручной деплой

Для GitHub Pages:

```bash
npm run build:pages
```

Затем загрузите файлы из `dist/` в ветку `gh-pages` или используйте GitHub Actions.

Для других хостингов:

```bash
npm run build
```

Загрузите файлы из `dist/` на хостинг (HTTPS обязателен).

### Развертывание в Telegram

1. После деплоя на GitHub Pages получите URL (например: `https://codesanitar.github.io/kids-safe-routes/`)
2. Создайте бота через [@BotFather](https://t.me/BotFather)
3. Настройте Mini App через команду `/newapp` в BotFather
4. Укажите URL вашего приложения

## Структура проекта

```
kids-safe-routes/
├── src/
│   ├── components/
│   │   ├── MapComponent.tsx    # Компонент карты
│   │   └── ControlPanel.tsx    # Панель управления
│   ├── services/
│   │   ├── ors.ts              # Сервис для работы с OpenRouteService API
│   │   ├── auth.ts             # Сервис авторизации
│   │   └── api.ts              # HTTP клиент для работы с API бэкенда
│   ├── utils/
│   │   └── geometry.ts         # Утилиты для работы с геометрией
│   ├── App.tsx                 # Главный компонент
│   ├── main.tsx                # Точка входа
│   └── types.ts                # TypeScript типы
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions workflow для деплоя
├── vite.config.ts              # Конфигурация Vite
├── .env.example               # Пример файла с переменными окружения
└── package.json
```

## Особенности

- **Автоматическое определение Telegram WebView**: Приложение автоматически определяет, запущено ли оно в Telegram, и скрывает баннер отладки
- **Авторизация через бэкенд**: При запуске приложение автоматически авторизуется через бэкенд, используя initData от Telegram
- **Хранение токенов**: AccessToken сохраняется в localStorage и автоматически используется во всех запросах к API
- **Обработка ошибок авторизации**: При неудачной авторизации пользователю показывается понятное сообщение об ошибке
- **Прокси для разработки**: В режиме разработки используется Vite proxy для обхода CORS при запросах к OpenRouteService API
- **Base path для GitHub Pages**: Автоматически настраивается правильный base path для деплоя на GitHub Pages

## Авторизация

Приложение использует авторизацию через бэкенд:

1. При запуске приложение получает `initData` от Telegram
2. Отправляется POST запрос на эндпоинт авторизации (`{VITE_API_BASE_URL}/auth`) с заголовками:
   - `x-init-data`: initData от Telegram
   - `Authorization: Bearer {VITE_AUTH_SECRET_KEY}`
3. В ответе ожидается JSON с полями:
   - `accessToken`: токен для последующих запросов
   - `user`: объект с данными пользователя (`tg_id`, `username`, `first_name`)
4. Токен сохраняется в localStorage и используется во всех последующих запросах к API через заголовок `Authorization: Bearer {accessToken}`

Если авторизация не удалась, функционал приложения будет недоступен, и пользователю будет показано сообщение "Не удалось авторизоваться в сервисе".

В режиме отладки (вне Telegram) авторизация пропускается.

**Примечание:** Эндпоинт авторизации (`/auth`) задан в коде. Если на вашем бэкенде используется другой путь, измените константу `AUTH_ENDPOINT` в файле `src/services/auth.ts`.

## Лицензия

MIT
